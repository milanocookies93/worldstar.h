#include <stdio.h>
#include "worldstar.h"

int isthree(int x);

int main()
{
	int var = 3;
	var turnup;
	var turndown;
	if (isthree(var))
		printf("Hello World\n");
	worldstar;
}

int isthree(int x)
{
	if (x==3)
		return TRU;
	else
		return NOOO;
}


